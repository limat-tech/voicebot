# In rasa/domain.yml
version: "3.1"

intents:
  - greet
  - search_product
  - add_to_cart
  - view_cart
  - go_to_checkout

entities:
  - product_name

# We are not using responses or actions from the domain yet, so you can leave those empty.
